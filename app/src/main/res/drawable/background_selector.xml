<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:drawable="@color/your_highlight_color" android:state_pressed="true"/>
    <!-- Focused state -->
    <item android:drawable="@color/colorFuture" android:state_focused="true"/>
    <!-- Default state -->
    <item android:drawable="@color/colorFuture"/>
</selector>
